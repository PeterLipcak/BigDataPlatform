START CONTAINERS:
docker-compose up

SCALE FLINK TASK MANAGERS:
docker-compose scale taskmanager=N

KAFKA CONSUMER:
kafka-console-consumer.sh --bootstrap-server localhost:29092 --topic anomalies

KAFKA PRODUCER:
kafka-console-producer.sh --broker-list localhost:29092 --topic consumptions < datasetFull.txt

docker-compose down -v
